
# 配置数据库连接信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///springboot_db?useSSL=false&serverTimezone=UTC
    username: root
    password: ganga
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tb_
# 整合mybatis-plus 表名与pojo类名不匹配问题